usage.header=Validates commit messages against lint rules.

lint.option.from=Initial commit (inclusive). If omitted, the default is HEAD.
lint.option.to=Final commit (inclusive). Optional when validating only one commit.
lint.parameter.rev-spec=Git SHA, branch, tag, or range in the format 'from..to'.